索引的类型：
	1、主键索引

	2、唯一索引

	3、普通索引

	4、全文索引

	5、组合索引

索引有效的查询：
	1、全职匹配

	2、最左前缀

	3、如果一个索引（如：组合索引）中包含所有要查询的字段的值，那么就称之为覆盖索引

	4、前缀索引
		字段内容过长，首先计算前多少位可以大概保持唯一性，然后创建索引时制定长度，弊端是无法使用group by 和order by
排序：
	1、排序字段不在索引列

	2、排序方向不一致

	3、where查询条件后为范围查询，无法使用到索引的其他列

	4、连表查询时，匹配第一个表的索引列